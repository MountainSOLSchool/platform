{
    "name": "portal-react",
    "$schema": "../../node_modules/nx/schemas/project-schema.json",
    "sourceRoot": "apps/portal-react",
    "projectType": "application",
    "targets": {
        "build": {
            "executor": "@nrwl/next:build",
            "outputs": ["{options.outputPath}"],
            "defaultConfiguration": "production",
            "options": {
                "root": "apps/portal-react",
                "outputPath": "dist/apps/portal-react"
            },
            "configurations": {
                "development": {
                    "outputPath": "apps/portal-react"
                },
                "production": {}
            }
        },
        "serve": {
            "executor": "@nrwl/next:server",
            "defaultConfiguration": "development",
            "options": {
                "buildTarget": "portal-react:build",
                "dev": true
            },
            "configurations": {
                "development": {
                    "buildTarget": "portal-react:build:development",
                    "dev": true
                },
                "production": {
                    "buildTarget": "portal-react:build:production",
                    "dev": false
                }
            }
        },
        "export": {
            "executor": "@nrwl/next:export",
            "options": {
                "buildTarget": "portal-react:build:production"
            }
        },
        "test": {
            "executor": "@nrwl/jest:jest",
            "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
            "options": {
                "jestConfig": "apps/portal-react/jest.config.ts",
                "passWithNoTests": true
            },
            "configurations": {
                "ci": {
                    "ci": true,
                    "codeCoverage": true
                }
            }
        },
        "lint": {
            "executor": "@nrwl/linter:eslint",
            "outputs": ["{options.outputFile}"],
            "options": {
                "lintFilePatterns": ["apps/portal-react/**/*.{ts,tsx,js,jsx}"]
            }
        }
    },
    "tags": []
}
