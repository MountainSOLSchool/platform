export function UnitSelectionTool(props) {
    return (
        <div>
            Behold all of the Mountain SOL Units!
            <div>Click on each unit to see the unit description.</div>;
        </div>
        // for each section, list units
        // each unit is clickable and opens up the description.
    );
}

export default UnitSelectionTool;
